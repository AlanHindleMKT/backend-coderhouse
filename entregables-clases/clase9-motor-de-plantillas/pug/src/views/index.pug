extends layout 

block contenido
    //-h2 Mi nombre es Alan
    h1 #{mensaje} #{nombre} #{apellido}
    if render
        h2 El deporte favorito es #{deporte}
    else
        h2 El deporte más aburrido es #{deporte}